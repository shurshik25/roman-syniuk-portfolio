name: Update Content and Deploy

on:
  # Ручний запуск
  workflow_dispatch:
  
  # Запуск від Адмін панелі
  repository_dispatch:
    types: [content-updated]

jobs:
  update-content:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build project
        run: npm run build
        
      - name: Update docs folder
        run: |
          rm -rf docs
          cp -r dist docs
          
      - name: Commit docs changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs/
          git commit -m "Auto-update: Rebuild and deploy with latest content" || exit 0
          # НЕ робимо git push - це створює нескінченний цикл!
          
      - name: Deploy to GitHub Pages
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🌐 Site will be updated in 5-10 minutes"
          echo "📝 Docs folder updated locally - GitHub Pages will use latest content"
